import React from "react";

const style = {
    paddingTop : "100px;",
    textAlign : "center"
};

export default () => 
    <div style={style}>
        <h3>Sorry, this page doesn't exist!</h3>
    </div>;
